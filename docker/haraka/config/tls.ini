# =============================================================================
# Haraka TLS Configuration (Development)
# =============================================================================
# Disabled for development - no TLS certificates

[main]
# Disable TLS in development
enabled=false

[outbound]
# Enable STARTTLS for outbound (opportunistic)
enable_tls=true
