# `npm version` inside this package tags as mcp-v<version> instead of v<version>,
# so the client's releases never collide with the application's own v<version>
# tags in this monorepo. The mcp-v tag is what triggers .github/workflows/publish-mcp.yml.
tag-version-prefix=mcp-v
